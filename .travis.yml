language: node_js
node_js:
- 10.16.0
cache:
  directories:
  - node_modules

jobs:
  include:
    - stage: test
      script: ./ci/test.sh
    - stage: build
      script: npm run build
    - state: deploy
      script: ./ci/build.sh
